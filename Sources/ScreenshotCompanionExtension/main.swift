import Foundation

@main
struct ScreenshotCompanionExtension {
    static func main() {
        NSLog("ScreenshotCompanionExtension is bundled as a nested .appex companion and is not launched directly.")
    }
}
